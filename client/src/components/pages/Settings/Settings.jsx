import ToggleMode from "../../atoms/ToggleMode/ToggleMode";

function Settings() {
    return (
        <div>
        <h1>FAQ</h1>
            <ToggleMode />
        </div>
    );
}

export default Settings;
